<<<<<<< HEAD
events { worker_connections 1024; }
http { 
  # configura um pool de endereço de servidores 
  upstream wordpress { 
      server wordpress1;
      server wordpress2;
      server wordpress3;
  }
  
  # configura esse servidor
  server { 
      listen 8080 default_server; # escutando por conexões na porta 8080
      listen [::]:8080 default_server;
      root /usr/share/nginx/html;
      index index.php;
      location / {
        add_header X-Upstream $upstream_addr;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header x-forwarded-for $proxy_add_x_forwarded_for;
  proxy_pass  http://wordpress;
      }
  }
}
=======
events {
    worker_connections 1024;
}

http {
    upstream wordpress {
        server wordpress1:80;
        server wordpress2:80;
        server wordpress3:80;
    }

    server {
        listen 8080 default_server;
        listen [::]:8080 default_server;
        root /usr/share/nginx/html;
        index index.php;

        location / {
            add_header X-Upstream $upstream_addr;
            try_files $uri $uri/ /index.php?$args;            
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header x-forwarded-for $proxy_add_x_forwarded_for;
            proxy_pass http://wordpress;
        }
    }
}
>>>>>>> 596673bfbc553639dd31b5b68093e0bbb29374b9
